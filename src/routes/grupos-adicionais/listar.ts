import {Router} from "express";import adaptRoute from "@/adapters/express-route-adapter";import Controller from "@/controllers/grupos-adicionais/listar";export default(router:Router):void=>{router.get("/grupos-adicionais",adaptRoute(new Controller()));};
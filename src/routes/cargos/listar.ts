import {Router} from "express";import adaptRoute from "@/adapters/express-route-adapter";import Controller from "@/controllers/cargos/listar";import {authMiddleware,authorizeRoles} from "@/middlewares";export default(router:Router):void=>{router.get("/cargos",authMiddleware,authorizeRoles(["Gerente"]),adaptRoute(new Controller()));};
import {Router} from "express";import adaptRoute from "@/adapters/express-route-adapter";import C from "@/controllers/pedido/buscar";import {authMiddleware,authorizeRoles} from "@/middlewares";export default(r:Router):void=>{r.get("/pedidos/:id",authMiddleware,authorizeRoles(["Gerente","Caixa","Entregador"]),adaptRoute(new C()));};
import {Router} from "express";import adaptRoute from "@/adapters/express-route-adapter";import Controller from "@/controllers/entregador/criar";import {authMiddleware,authorizeRoles} from "@/middlewares";export default(router:Router):void=>{router.post("/entregadores",authMiddleware,authorizeRoles(["Gerente"]),adaptRoute(new Controller()));};
import {badRequest,created,serverError} from "@/helpers/http-helper";import {Controller,HttpRequest,HttpResponse} from "@/protocols";import s from "@/service/pedido-service";export default class C implements Controller{async handle(r:HttpRequest):Promise<HttpResponse>{try{return created(await s.criar(r.body??{}));}catch(e){if(e instanceof Error)return badRequest(e);return serverError(e);}}}
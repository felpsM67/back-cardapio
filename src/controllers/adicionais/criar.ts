import {badRequest,created,serverError} from "@/helpers/http-helper";import {Controller,HttpRequest,HttpResponse} from "@/protocols";import adicionaisService from "@/service/adicionais-service";export default class CriarController implements Controller{async handle(r:HttpRequest):Promise<HttpResponse>{try{return created(await adicionaisService.criarAdicional(r.body??{}));}catch(e){if(e instanceof Error)return badRequest(e);return serverError(e);}}}
import {badRequest,created,serverError} from "@/helpers/http-helper";import {Controller,HttpRequest,HttpResponse} from "@/protocols";import entregadorService from "@/service/entregador-service";export default class CriarController implements Controller{async handle(r:HttpRequest):Promise<HttpResponse>{try{return created(await entregadorService.criar(r.body??{}));}catch(e){if(e instanceof Error)return badRequest(e);return serverError(e);}}}
import {badRequest,created,serverError} from "@/helpers/http-helper";import {Controller,HttpRequest,HttpResponse} from "@/protocols";import funcionarioService from "@/service/funcionario-service";export default class CriarController implements Controller{async handle(r:HttpRequest):Promise<HttpResponse>{try{return created(await funcionarioService.criar(r.body??{}));}catch(e){if(e instanceof Error)return badRequest(e);return serverError(e);}}}